*.back*
*.bak*
result
# Per-device TOML metadata (machine target + sops key label)
.device-label
# Sops encrypted secrets — safe to commit (encrypted with age)
# secrets/secrets.yaml
# Old secrets.nix (replaced by machine config + sops)
/secrets.nix
__pycache__
