*.back
*.env
*.txt
.tmp
result
# Non-sensitive config (generated by setup.sh, same format as old secrets.nix)
config.*
# Per-device identity (hostname-based label for sops keys)
.device-label
# Sops encrypted secrets — safe to commit (encrypted with age)
# secrets/secrets.yaml
# Old secrets.nix (replaced by config.nix + sops)
secrets.nix
__pycache__
