*.back
*.env
*.txt
.tmp
result
# Non-sensitive config (generated by setup.sh, same format as old secrets.nix)
config.*
# Sops encrypted secrets — safe to commit once encrypted, but keep gitignored during dev
secrets/secrets.yaml
# Old secrets.nix (replaced by config.nix + sops)
secrets.nix
__pycache__
